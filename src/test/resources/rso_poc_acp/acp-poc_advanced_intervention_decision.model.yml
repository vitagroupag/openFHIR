engine: FHIRConnect/v0.0.1
type: model
metadata:
  name: "openEHR-EHR-EVALUATION.advance_intervention_decisions.v1"
  version: 0.0.1a # version of this particular mapping
spec: # schema specific to the FHIRConnect v0.0.1 engine
  system: FHIR
  version: R4
  openEhrConfig:
    archetype: "openEHR-EHR-EVALUATION.advance_intervention_decisions.v1"
  fhirConfig:
    structureDefinition: http://hl7.org/fhir/StructureDefinition/Observation
    condition:
      - targetRoot: "$resource"
        targetAttribute: "meta.profile"
        operator: "one of"
        criteria: "[http://nictiz.nl/fhir/StructureDefinition/zib-TreatmentDirective]"

mappings:

  - name: "treatment"
    with:
      fhir: "$resource.extension"
      openehr: "$archetype.advance_intervention_decisions.per_intervention.intervention"
      type: "CODEABLECONCEPT"
    fhirCondition:
      targetRoot: "$resource.extension"
      targetAttribute: "url"
      operator: "one of"
      criteria: "[http://nictiz.nl/fhir/StructureDefinition/zib-TreatmentDirective-Treatment]"

  - name: "decision"
    with:
      fhir: "$resource.modifierExtension"
      openehr: "$archetype.advance_intervention_decisions.per_intervention.decision"
      type: "CODEABLECONCEPT"
    fhirCondition:
      targetRoot: "$resource.modifierExtension"
      targetAttribute: "url"
      operator: "one of"
      criteria: "[http://nictiz.nl/fhir/StructureDefinition/zib-TreatmentDirective-TreatmentPermitted]"

  - name: "comment"
    with:
      fhir: "$resource.extension"
      openehr: "$archetype.advance_intervention_decisions.per_intervention.comment"
      type: "TEXT"
    fhirCondition:
      targetRoot: "$resource.extension"
      targetAttribute: "url"
      operator: "one of"
      criteria: "[http://nictiz.nl/fhir/StructureDefinition/Comment]"