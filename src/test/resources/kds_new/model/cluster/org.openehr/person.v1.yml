engine: FHIRConnect/v0.0.1
type: model
metadata:
  name:  CLUSTER.person.v1
  version: 0.0.1a # version of this particular mapping
spec: # schema specific to the FHIRConnect v0.0.1 engine
  system: FHIR
  version: R4
  openEhrConfig:
    archetype: openEHR-EHR-CLUSTER.person.v1
  fhirConfig:
    structureDefinition: http://hl7.org/fhir/StructureDefinition/BackboneElement #if you put Person, it will map it again


mappings:
  - name: "name"
    with:
      fhir: "$resource.name"
      openehr: "$archetype/content[openEHR-EHR-ADMIN_ENTRY.person_data.v0]/data[at0001]/items[openEHR-EHR-CLUSTER.person.v1]/items[openEHR-EHR-CLUSTER.structured_name.v1, 'Name']"
    slotArchetype: "CLUSTER.structured_name.v1.person_name-structured_name"

  - name: "address"
    with:
      fhir: "$resource.address"
      openehr: "$archetype/content[openEHR-EHR-ADMIN_ENTRY.person_data.v0]/data[at0001]/items[openEHR-EHR-CLUSTER.person.v1]/items[openEHR-EHR-CLUSTER.address.v1, 'Straßenanschrift / Postfach']"
    slotArchetype: "CLUSTER.address.v1"

  - name: "organizationReference"
    with:
      fhir: "managingOrganization"
      openehr: "$reference"
    reference:
      resourceType: "Organization"
      mappings:
        - name: "organization"
          with:
            fhir: "$fhirRoot"
            openehr: "$archetype/content[openEHR-EHR-ADMIN_ENTRY.person_data.v0]/data[at0001]/items[openEHR-EHR-CLUSTER.person.v1]/items[openEHR-EHR-CLUSTER.organisation.v1, 'Verwaltungsorganisation']"
          slotArchetype: "CLUSTER.organisation.v1"
          #todo: identifier_value would need to be overwritten in a KDS organisation if we wanted to map that one

#  - name: "electronicCommunication" #todo: where in a template is this?!
#    with:
#      fhir: "$resource"
#      openehr: "$archetype/items[openEHR-EHR-CLUSTER.electronic_communication.v1]"
#    slotArchetype: "CLUSTER.electronic_communication.v1"
