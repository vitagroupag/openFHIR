engine: FHIRConnect/v0.0.1
type: model
metadata:
  name: CLUSTER.observation_status_fhir.v1
  version: 0.0.1a
spec:
  system: FHIR
  version: R4
  openEhrConfig:
    archetype: openEHR-EHR-CLUSTER.observation_status_fhir.v1

mappings:

  - name: "status"
    with:
      fhir: "$fhirRoot.status"
      openehr: "$archetype/items[at0015]"

  - name: "code"
    with:
      fhir: "$fhirRoot.code"
      openehr: "$archetype/items[at0014]"

