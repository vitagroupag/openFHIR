engine: FHIRConnect/v0.0.1
type: model
metadata:
  name: ACTION.informed_consent.v0
  version: 0.0.1a # version of this particular mapping
spec: # schema specific to the FHIRConnect v0.0.1 engine
  system: FHIR
  version: R4
  openEhrConfig:
    archetype: openEHR-EHR-ACTION.informed_consent.v0
  fhirConfig:
    structureDefinition: http://hl7.org/fhir/StructureDefinition/Consent


mappings:

  - name: "contextStartTime"
    with:
      fhir: "$resource.dateTime"
      openehr: "$archetype/context/start_time"
      type: "DATETIME"

  - name: "healthCareFacility"
    with:
      fhir: "$resource.organization"
      openehr: "$archetype/context/_health_care_facility|name"
      type: "STRING"

#  - name: "ISMTransition"
#    with:
#      fhir: "$resource" #todo there needs to be a mapping table
#      openehr: "$archetype/ism_transition"


  - name: "studie"
    with:
      fhir: "$resource"
      openehr: "$archetype/content[openEHR-EHR-ACTION.informed_consent.v0]/description[at0001]/items[openEHR-EHR-CLUSTER.study_participation.v1]"
    slotArchetype: "CLUSTER.study_participation.v1"


#waiting for template update