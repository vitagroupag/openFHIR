engine: FHIRConnect/v0.0.1
type: model
metadata:
  name:  ACTION.medication.v1
  version: 0.0.1a # version of this particular mapping
spec: # schema specific to the FHIRConnect v0.0.1 engine
  system: FHIR
  version: R4
  openEhrConfig:
    archetype: openEHR-EHR-ACTION.medication.v1
  fhirConfig:
    structureDefinition: http://hl7.org/fhir/StructureDefinition/MedicationAdministration

mappings:
#  - name: "hierarchyMappings" # is defined by the
#    with:
#      fhir: "$resource.dosage"
#      openEHR: "$archetype
#    #    type: "HIERARCHY"
#    hierarchy:
#      fhirCreate: "resource"
#      openehrCreate: "archetype"
#      uniqueValueFHIR: "[route, site]" # if route value is different per dosage slice make a new archetype
#      uniqueValueOpenEHR: "['/data[at0003]/items[openEHR-EHR-CLUSTER.medication.v2]/items[at0132]']"

#  - name: "healthcareFacility"
#    with:
#      fhir: "$resource.requester.as(Organization).display" #todo: no such fhir attribute
#      openehr: "$archetype.context._health_care_facility|name"
#      type: "STRING"

  - name: "context"
    with:
      fhir: "$resource.effective.as(Period)"
      openehr: "$composition/context"
    followedBy:
      mappings:
        - name: "contextStart"
          with:
            fhir: "start"
            openehr: "start_time"
            type: "DATETIME"

        - name: "contextEnd"
          with:
            fhir: "end"
            openehr: "_end_time"
            type: "DATETIME"



#  - name: "composer" #TODO introduce unidirectional mapping or just take the last one ?!
#    with:
#      fhir: "$resource.request.requester" # take identifier
#      openehr: "$archetype/composer"
#
#  - name: "provider"
#    with:
#      fhir: "$resource.actor.performer"
#      openehr: "$archetype/provider"


# category mappings to fhir based on openehr value

  - name: "category_outpatient_fhir_parent"
    with:
      fhir: "$resource.category.coding"
      openehr: "$archetype"
      type: "NONE"
    openehrCondition:
      targetRoot: "$archetype"
      targetAttributes:
        - "context/setting/defining_code"
      operator: "one of"
      criteria: "[225]"
    followedBy:
      mappings:
        - name: "category_outpatient_fhir_code"
          with:
            fhir: "code"
            value: "outpatient"
        - name: "category_outpatient_fhir_system"
          with:
            fhir: "system"
            value: "http://terminology.hl7.org/CodeSystem/medication-admin-category"

  - name: "category_inpatient_fhir_parent"
    with:
      fhir: "$resource.category.coding"
      openehr: "$archetype"
      type: "NONE"
    openehrCondition:
      targetRoot: "$archetype"
      targetAttributes:
        - "context/setting/defining_code"
      operator: "one of"
      criteria: "[228]"
    followedBy:
      mappings:
        - name: "category_outpatient_fhir_code"
          with:
            fhir: "code"
            value: "inpatient"
        - name: "category_outpatient_fhir_system"
          with:
            fhir: "system"
            value: "http://terminology.hl7.org/CodeSystem/medication-admin-category"

  - name: "category_community_fhir_parent"
    with:
      fhir: "$resource.category.coding"
      openehr: "$archetype"
      type: "NONE"
    openehrCondition:
      targetRoot: "$archetype"
      targetAttributes:
        - "context/setting/defining_code"
      operator: "one of"
      criteria: "[238]"
    followedBy:
      mappings:
        - name: "category_outpatient_fhir_code"
          with:
            fhir: "code"
            value: "community"
        - name: "category_outpatient_fhir_system"
          with:
            fhir: "system"
            value: "http://terminology.hl7.org/CodeSystem/medication-admin-category"
  # END OF category mappings to fhir based on openehr value
  # category mappings to openehr based on fhir value
  - name: "category_outpatient_openehr_code"
    with:
      openehr: "$composition/context/setting/defining_code"
      value: "225"
    fhirCondition:
      targetRoot: "$resource"
      targetAttribute: "category.coding.code"
      operator: "one of"
      criteria: "[outpatient]"
  - name: "category_outpatient_openehr_value"
    with:
      openehr: "$composition/context/setting/value"
      value: "Outpatient"
    fhirCondition:
      targetRoot: "$resource"
      targetAttribute: "category.coding.code"
      operator: "one of"
      criteria: "[outpatient]"
  - name: "category_outpatient_openehr_terminology"
    with:
      openehr: "$composition/context/setting/terminology_id"
      value: "local"
    fhirCondition:
      targetRoot: "$resource"
      targetAttribute: "category.coding.code"
      operator: "one of"
      criteria: "[outpatient]"

  - name: "category_inpatient_openehr_code"
    with:
      openehr: "$composition/context/setting/defining_code"
      value: "228"
    fhirCondition:
      targetRoot: "$resource"
      targetAttribute: "category.coding.code"
      operator: "one of"
      criteria: "[inpatient]"
  - name: "category_inpatient_openehr_value"
    with:
      openehr: "$composition/context/setting/value"
      value: "Inpatient"
    fhirCondition:
      targetRoot: "$resource"
      targetAttribute: "category.coding.code"
      operator: "one of"
      criteria: "[inpatient]"
  - name: "category_inpatient_openehr_terminology"
    with:
      openehr: "$composition/context/setting/terminology_id"
      value: "local"
    fhirCondition:
      targetRoot: "$resource"
      targetAttribute: "category.coding.code"
      operator: "one of"
      criteria: "[inpatient]"

  - name: "category_community_openehr_code"
    with:
      openehr: "$composition/context/setting/defining_code"
      value: "238"
    fhirCondition:
      targetRoot: "$resource"
      targetAttribute: "category.coding.code"
      operator: "one of"
      criteria: "[community]"
  - name: "category_community_openehr_value"
    with:
      openehr: "$composition/context/setting/value"
      value: "Community"
    fhirCondition:
      targetRoot: "$resource"
      targetAttribute: "category.coding.code"
      operator: "one of"
      criteria: "[community]"
  - name: "category_community_openehr_terminology"
    with:
      openehr: "$composition/context/setting/terminology_id"
      value: "local"
    fhirCondition:
      targetRoot: "$resource"
      targetAttribute: "category.coding.code"
      operator: "one of"
      criteria: "[community]"

  - name: "note"
    with:
      fhir: "$resource.note.text"
      openehr: "$archetype/description[at0017]/items[at0024]"
      type: "STRING"

  - name: "dosage"
    with:
      fhir: "$resource.dosage"
      openehr: "$archetype/description[at0017]/items[openEHR-EHR-CLUSTER.dosage.v2]"
    slotArchetype: "CLUSTER.dosage.v2.BackboneElement-to-dosage" #todo: why CLUSTER.dosage.v2.BackboneElement-to-dosage and not CLUSTER.dosage.v2.dosage-to-dosage
    followedBy:
      mappings:
        - name: "route"
          with:
            fhir: "route"
            openehr: "$archetype/description[at0017]/items[at0140]/items[at0147]"
            type: "CODEABLECONCEPT"

        - name: "bodySite"
          with:
            fhir: "site"
            openehr: "$archetype/description[at0017]/items[at0140]/items[at0141]"
            type: "CODEABLECONCEPT"

        - name: "method"
          with:
            fhir: "method"
            openehr: "$archetype/description[at0017]/items[at0140]/items[at0143]"
            type: "CODEABLECONCEPT"

  - name: "medication"
    with:
      fhir: "$resource.medication"
      openehr: "$reference"
    reference:
      resourceType: "Medication"
      mappings:
        - name: "medicationReference"
          with:
            fhir: "$fhirRoot"
            openehr: "$archetype/description[at0017]/items[openEHR-EHR-CLUSTER.medication.v2]"
          slotArchetype: "CLUSTER.medication.v2.medication-to-medication" # depending on the resourceType picks the correct one


  - name: "medicationCode"
    with:
      fhir: "$resource.medication.as(CodeableConcept).text"
      openehr: "$archetype/description[at0017]/items[openEHR-EHR-CLUSTER.medication.v2]/items[at0132]"
      type: "STRING"
    openehrCondition:
      targetRoot: "$archetype/description[at0017]/items[openEHR-EHR-CLUSTER.medication.v2]"
      targetAttributes:
        - "items[at0071]" #"darreichungsform"
        - "items[at0115]" #"wirkstärke_konzentration"
      operator: "empty"


  - name: "klinischeIndikation"
    with:
      fhir: "$resource.reasonCode.text"
      openehr: "$archetype/description[at0017]/items[at0156]"
      type: "STRING"

#  - name: "partOf"
#    with:
#      fhir: "$resource.partOf.reference"
#      openehr: "$reference"
#    reference:
#      mappings:
#        - name: "partOfReference"
#          with:
#            fhir: "$fhirRoot"
#            openehr: "$archetype/links" # will use link
#            type: "REFERENCED_MAPPING" # execute mapping or use already mapped one to get Composition URI
#            openehrLink:
#              meaning: "part of a other medication administration"
#              type: "partOf"

  - name: "request" # always the last one is executed for the mapping back.
    with:
      fhir: "$resource.request.as(Reference).identifier.value"
      openehr: "$archetype/protocol[at0030]/items[at0103]"
      type: "STRING"
#        - name: "encounterMapping"
#          with:
#            fhir: "$fhirRoot"
#            openehr: "$archetype/protocol[at0030]/items[at0103]/links" # will use link
#            type: "REFERENCED_MAPPING" # execute mapping or use already mapped one to get Composition URI if there is any
#            openehrLink:
#              meaning: "the medication request for this medication"
#              type: "medicationRequest"


  # ism transition hardcoding to fhir based on openEhr value

  - name: "ISMTransition_active_fhir"
    with:
      fhir: "$resource.status"
      value: "active"
    openehrCondition:
      targetRoot: "$archetype"
      targetAttributes:
        - "ism_transition/current_state/defining_code"
      operator: "one of"
      criteria: "[245]"

  - name: "ISMTransition_rejected_fhir"
    with:
      fhir: "$resource.status"
      value: "rejected"
    openehrCondition:
      targetRoot: "$archetype"
      targetAttributes:
        - "ism_transition/current_state/defining_code"
      operator: "one of"
      criteria: "[528]"

  - name: "ISMTransition_inactive_fhir"
    with:
      fhir: "$resource.status"
      value: "inactive"
    openehrCondition:
      targetRoot: "$archetype"
      targetAttributes:
        - "ism_transition/current_state/defining_code"
      operator: "one of"
      criteria: "[532]"

  - name: "ISMTransition_error_fhir"
    with:
      fhir: "$resource.status"
      value: "entered-in-error"
    openehrCondition:
      targetRoot: "$archetype"
      targetAttributes:
        - "ism_transition/current_state/defining_code"
      operator: "one of"
      criteria: "[528]"
  # END of ism transition hardcoding to fhir based on openEhr value


  # ism transition hardcoding to openehr based on fhir value

  - name: "ISMTransition_active_openehr"
    with:
      openehr: "$archetype/ism_transition/current_state/defining_code"
      value: "245"
    fhirCondition:
      targetRoot: "$fhirRoot"
      targetAttribute: "status"
      operator: "one of"
      criteria: "[active]"
  - name: "ISMTransition_active_openehr_terminology"
    with:
      openehr: "$archetype/ism_transition/current_state/terminology_id"
      value: "local"
    fhirCondition:
      targetRoot: "$fhirRoot"
      targetAttribute: "status"
      operator: "one of"
      criteria: "[active]"
  - name: "ISMTransition_active_openehr_value"
    with:
      openehr: "$archetype/ism_transition/current_state/value"
      value: "Active"
    fhirCondition:
      targetRoot: "$fhirRoot"
      targetAttribute: "status"
      operator: "one of"
      criteria: "[active]"

  - name: "ISMTransition_inactive_openehr"
    with:
      openehr: "$archetype/ism_transition/current_state/defining_code"
      value: "532"
    fhirCondition:
      targetRoot: "$fhirRoot"
      targetAttribute: "status"
      operator: "one of"
      criteria: "[inactive]"
  - name: "ISMTransition_entered_in_error_openehr"
    with:
      openehr: "$archetype/ism_transition/current_state/defining_code"
      value: "531"
    fhirCondition:
      targetRoot: "$fhirRoot"
      targetAttribute: "status"
      operator: "one of"
      criteria: "[entered-in-error]"